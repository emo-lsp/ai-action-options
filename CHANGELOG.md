# 更新记录

## 1.6.4（2026-09-18）

修复生成行动选项期间，进度弹窗边框流光显示但不移动的问题。

- 修复生产压缩 Sass 的 BOM 进入内联 `<style>` 后，破坏首条 `@property` 注册的问题。
- 内联 Sass 构建统一关闭 charset 标记，让流光角度恢复连续插值。
- 新增生产样式回归检查，防止 BOM 再次进入内联 CSS。

本版已完成定向测试、生产构建检查及实际页面流光确认。

## 1.6.3（2026-09-17）

正式版，合并此前开发及测试阶段的功能和修复。更新清单仅保留 1.6.3，不包含测试占位功能。

### 更新中心

- 新增自动检查、更新红点、版本说明、指定版本安装及 SHA-256 校验。
- 支持 TestingCF jsDelivr、jsDelivr CDN、GitHub Raw；自动模式比较可用清单，避免旧 CDN 缓存截断列表。
- 手动检查绕过本地 24 小时缓存，不会自动安装。
- 使用同主题确认弹窗，“发现新版本”改用蓝色信息态。
- 脚本替换、成功通知及刷新由宿主页面执行，避免旧 iframe 卸载中断回调；全局脚本等待保存后显示通知，再刷新整页。

### 剧情与生成

- 历史剧情摘要读取、自动识别或自定义标签，以及逐楼层预览。
- 完整读取所选 AI 回复，避免最新楼层重复；稳定上下文前缀顺序。
- 过滤 UpdateVariable、Analysis、JSON Patch 等内部块，兼容嵌套或未闭合内容。
- 改进生成进度分组、移动端紧凑布局和摘要预览。

### 升级注意

- 从旧测试版首次升级可能仍受旧安装器影响；写入后未自动刷新时需手动刷新一次。
- 本版通过定向测试、静态检查和生产构建。浏览器、真机及预设/角色卡慢速保存流程仍需复验。
